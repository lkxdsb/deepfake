model:
  class_path: src.model.clip.evl.EfficientVideoLearner
  init_args:
    num_frames: 10
    architecture: ViT-L/14
    # CCSA 配置：EVL Decoder 使用 CCSA（根据架构图）
    enable_ccsa: true
trainer:
  accumulate_grad_batches: 1