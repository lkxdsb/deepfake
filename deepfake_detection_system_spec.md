# 深度伪造检测系统技术说明书（供 Codex 继续完善）

## 1. 项目定位

本项目拟参加 **中国大学生计算机设计大赛 - 人工智能应用** 赛道，目标是构建一个可运行、可演示、可部署的 **深度伪造（Deepfake）检测系统**，用于对上传的图片或视频进行真假判别，并输出可解释的检测结果。

本系统的重点不是做一个复杂的商业网站，而是：

1. **把已有模型权重真正接入系统**；
2. **形成完整的检测闭环**：输入 -> 预处理 -> 推理 -> 结果可视化 -> 记录保存；
3. **满足比赛展示需求**：系统可运行、可录制演示视频、可写技术文档、可说明方案设计；
4. **便于后续由 Codex/开发助手继续扩展**。

---

## 2. 比赛导向约束

### 2.1 赛道建议

当前项目更适合按 **人工智能应用** 赛道准备，而不是把大量精力花在复杂前端工程上。

人工智能应用赛道更强调：

- 完整的方案设计
- 代码实现
- 技术文档
- 作品源代码
- 用户手册
- 功能演示视频
- 现场/录屏演示系统功能

因此，本系统应优先保证：

- 模型接入完整
- 推理结果可信
- 界面可演示
- 文档可提交
- 系统部署稳定

### 2.2 若改报 Web 应用与开发

如果后续改为 **软件应用与开发 -> Web应用与开发**，则需要额外满足：

- 系统必须提供 **可通过互联网访问的网址**；
- 若无法提供公网可访问地址，可能影响资格审查。

因此，当前设计默认以 **AI 应用赛道** 为主，但架构仍保留 Web 化部署能力。

---

## 3. 项目目标

### 3.1 核心目标

构建一个面向比赛展示的深度伪造检测系统，实现以下能力：

- 支持上传图片/视频进行检测；
- 调用已有模型权重完成推理；
- 输出真假预测结果、置信度、关键帧/热力图/可解释信息；
- 支持查看检测历史；
- 支持演示模式和部署模式；
- 支持本地开发 + 云端推理部署。

### 3.2 非核心目标

以下内容不是当前优先级最高的工作：

- 复杂权限系统
- 商业化支付/订单功能
- 多租户 SaaS 架构
- 过度复杂的前后端分离
- 高并发生产级集群

比赛阶段应遵循 **够用、稳定、可展示、能讲清楚** 的原则。

---

## 4. 目标用户

### 4.1 比赛评委

希望在短时间内看到：

- 系统能跑起来；
- 模型不是“PPT 模型”；
- 输入输出链路完整；
- 结果可视化清晰；
- 有一定创新点和实用价值。

### 4.2 普通演示用户

希望快速完成：

- 上传文件
- 点击检测
- 查看结果
- 理解检测依据

### 4.3 开发维护者

希望系统具备：

- 清晰目录结构
- 易替换模型
- 易扩展接口
- 易部署
- 易录制演示视频

---

## 5. 业务场景

### 5.1 图片检测场景

用户上传单张图片，系统返回：

- 是否疑似深度伪造
- 置信度
- 可解释区域（如热力图/注意力图）
- 推理耗时

### 5.2 视频检测场景

用户上传短视频，系统返回：

- 视频级真假判定
- 关键帧检测结果
- 帧级概率曲线
- 可解释截图或热力图
- 总处理耗时

### 5.3 演示答辩场景

演示人员可使用准备好的测试样例，展示：

- 真实样本与伪造样本的检测差异
- 系统的完整推理流程
- 检测结果页面
- 模型能力与局限性说明

---

## 6. 系统总体架构

建议采用 **轻前端 + 推理后端 + 模型服务** 的架构。

```text
[浏览器前端 / 演示界面]
        |
        v
[Web API 后端 FastAPI]
        |
        +--> 文件上传管理
        +--> 任务调度
        +--> 推理调用
        +--> 结果持久化
        |
        v
[模型推理模块]
        |
        +--> 图像预处理
        +--> 视频抽帧
        +--> 模型加载
        +--> 批量推理
        +--> 结果聚合
        |
        v
[存储层]
        +--> 原始上传文件
        +--> 中间结果
        +--> 最终结果 JSON
        +--> 可视化图片/关键帧
```

---

## 7. 部署建议

### 7.1 推荐方案

比赛阶段推荐采用：

- **AutoDL / 云GPU服务器**：部署后端和模型推理服务
- **轻量前端**：用于展示上传、检测、结果可视化

### 7.2 开发模式

#### 模式 A：本地联调

- 前端在本地运行
- 后端在本地或服务器运行
- 模型可先本地测试，再迁移到云端

#### 模式 B：比赛演示模式（推荐）

- 前端和后端尽量统一部署在同一台服务器
- 模型权重放在服务器本地磁盘
- 演示时只打开一个公网入口

### 7.3 不推荐方案

不建议比赛当天使用“本地前端 + 本地网络连接远程推理 + 临时转发端口”的脆弱方案，因为容易出现：

- 网络不稳定
- 跨域问题
- 端口映射失败
- 环境不一致
- 演示录屏与现场环境不同

---

## 8. 功能需求

### 8.1 首页

功能：

- 项目简介
- 支持的检测类型说明（图片/视频）
- 上传入口
- 示例样本入口

### 8.2 图片检测模块

功能：

- 上传 jpg/png/jpeg 图片
- 文件大小校验
- 自动预处理
- 模型推理
- 显示分类结果
- 显示置信度
- 显示热力图/注意力图（如可提供）
- 下载结果报告（可选）

### 8.3 视频检测模块

功能：

- 上传 mp4/avi/mov 视频
- 视频合法性校验
- 自动抽帧
- 帧级推理
- 视频级结果聚合
- 展示关键帧
- 展示概率曲线（可选）
- 显示总耗时

### 8.4 示例演示模块

功能：

- 内置真实样本
- 内置伪造样本
- 一键运行示例
- 快速进入结果页

这个模块非常适合比赛答辩，避免临时上传文件浪费时间。

### 8.5 历史记录模块

功能：

- 查看历史检测列表
- 查看时间、文件名、结果、置信度
- 点击进入详情页

### 8.6 结果详情模块

功能：

- 原始文件预览
- 预测结果
- 置信度
- 可解释图
- 关键帧结果
- 模型版本
- 推理时间
- 系统备注

---

## 9. 非功能需求

### 9.1 可用性

- 页面操作尽量简单
- 3 次点击内完成检测
- 错误提示清晰

### 9.2 稳定性

- 上传失败时给出重试提示
- 推理失败时返回明确错误原因
- 模型未加载时提示而不是崩溃

### 9.3 可维护性

- 模型代码与 Web 代码解耦
- 预处理逻辑单独封装
- 配置项统一管理
- API 文档自动生成

### 9.4 可扩展性

- 后续支持多个模型切换
- 后续支持更多伪造类型
- 后续支持批量检测

---

## 10. 推荐技术栈

### 10.1 后端

- **Python 3.10+**
- **FastAPI**：接口服务
- **Uvicorn**：运行服务
- **Pydantic**：数据校验
- **OpenCV**：视频处理/抽帧
- **PyTorch**：模型推理
- **NumPy / Pillow**：图像处理

### 10.2 前端

二选一：

#### 方案 1：极简快速型（推荐比赛阶段）

- HTML + CSS + JavaScript
- 或 Jinja2 模板 + FastAPI 静态页面

优点：

- 维护成本低
- 易部署
- 适合比赛演示

#### 方案 2：前后端分离型

- Vue / React
- Axios 调用后端 API

优点：

- 页面更现代
- 交互更灵活

缺点：

- 工程复杂度更高
- 比赛阶段不一定划算

### 10.3 数据存储

比赛阶段可采用轻量方案：

- SQLite：保存检测记录
- 本地文件系统：保存上传文件、热力图、关键帧、结果 JSON

后续可扩展：

- MySQL / PostgreSQL
- MinIO / OSS

---

## 11. 推荐目录结构

```text
project/
├── app/
│   ├── main.py                  # FastAPI 入口
│   ├── api/
│   │   ├── routes_detect.py     # 检测接口
│   │   ├── routes_history.py    # 历史记录接口
│   │   └── routes_demo.py       # 示例演示接口
│   ├── core/
│   │   ├── config.py            # 配置文件
│   │   ├── logger.py            # 日志
│   │   └── security.py          # 可选安全控制
│   ├── services/
│   │   ├── image_service.py     # 图片检测服务
│   │   ├── video_service.py     # 视频检测服务
│   │   ├── inference_service.py # 统一推理调度
│   │   └── result_service.py    # 结果保存与读取
│   ├── models/
│   │   ├── schemas.py           # Pydantic 模型
│   │   └── db_models.py         # 数据库模型
│   ├── utils/
│   │   ├── file_utils.py
│   │   ├── image_utils.py
│   │   ├── video_utils.py
│   │   └── vis_utils.py
│   ├── templates/               # 若使用服务端渲染
│   └── static/
├── weights/
│   └── best_model.pth
├── outputs/
│   ├── uploads/
│   ├── heatmaps/
│   ├── frames/
│   └── reports/
├── docs/
│   ├── api.md
│   ├── deploy.md
│   └── user_manual.md
├── scripts/
│   ├── test_infer.py
│   ├── export_demo_data.py
│   └── benchmark.py
├── requirements.txt
├── README.md
└── run.sh
```

---

## 12. 推理流程设计

### 12.1 图片流程

```text
上传图片
 -> 文件校验
 -> 图像读取
 -> 预处理
 -> 模型推理
 -> 概率输出
 -> 阈值判定
 -> 结果可视化
 -> 保存记录
 -> 返回前端
```

### 12.2 视频流程

```text
上传视频
 -> 文件校验
 -> 抽帧
 -> 人脸检测/ROI 提取（如需要）
 -> 帧级预处理
 -> 批量推理
 -> 帧级结果聚合
 -> 视频级判定
 -> 生成关键帧图/曲线图
 -> 保存记录
 -> 返回前端
```

---

## 13. 模型接入要求

### 13.1 已知前提

当前已经具备：

- 模型权重
- 训练代码
- 推理代码

### 13.2 接入目标

需要把现有推理脚本改造成 **可被 Web 服务调用的统一函数**。

建议统一接口如下：

```python
def predict_image(image_path: str) -> dict:
    return {
        "label": "fake",
        "score": 0.93,
        "heatmap_path": "outputs/heatmaps/xxx.png",
        "inference_time": 0.12,
        "model_name": "xxx"
    }
```

```python
def predict_video(video_path: str) -> dict:
    return {
        "label": "fake",
        "score": 0.89,
        "frame_results": [],
        "keyframes": [],
        "curve_path": "outputs/reports/xxx_curve.png",
        "inference_time": 3.8,
        "model_name": "xxx"
    }
```

### 13.3 模型接入原则

- 不要把推理逻辑直接写死在 API 路由里；
- 模型加载应尽量在服务启动后复用，避免每次请求重复加载；
- 对于视频任务，尽量支持批量推理；
- 输出结果结构必须统一，方便前端显示。

---

## 14. API 设计建议

### 14.1 健康检查

```http
GET /api/health
```

返回：

```json
{
  "status": "ok",
  "model_loaded": true
}
```

### 14.2 图片检测

```http
POST /api/detect/image
```

返回：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "task_id": "img_001",
    "label": "fake",
    "score": 0.93,
    "heatmap_url": "/outputs/heatmaps/img_001.png",
    "inference_time": 0.12
  }
}
```

### 14.3 视频检测

```http
POST /api/detect/video
```

返回：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "task_id": "vid_001",
    "label": "real",
    "score": 0.18,
    "keyframes": ["/outputs/frames/1.jpg", "/outputs/frames/2.jpg"],
    "curve_url": "/outputs/reports/vid_001_curve.png",
    "inference_time": 4.52
  }
}
```

### 14.4 历史列表

```http
GET /api/history
```

### 14.5 历史详情

```http
GET /api/history/{task_id}
```

---

## 15. 数据表建议

### 15.1 detection_records

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | int | 主键 |
| task_id | varchar | 任务编号 |
| file_name | varchar | 原始文件名 |
| file_type | varchar | image/video |
| result_label | varchar | real/fake |
| score | float | 置信度 |
| source_path | varchar | 原文件路径 |
| result_json_path | varchar | 结果 JSON 路径 |
| preview_path | varchar | 预览图路径 |
| created_at | datetime | 创建时间 |
| model_version | varchar | 模型版本 |
| inference_time | float | 推理耗时 |

---

## 16. 页面设计建议

### 16.1 首页

建议展示：

- 标题：深度伪造检测系统
- 简介：用于图像/视频的 AI 伪造内容检测
- 两个按钮：图片检测 / 视频检测
- 一个按钮：示例演示

### 16.2 结果页

建议展示：

- 文件预览
- 结果标签（真 / 假）
- 置信度进度条
- 可解释图
- 关键说明
- 推理耗时

### 16.3 UI 风格

- 简洁、科技感
- 颜色不宜过多
- 重点突出结论和可视化
- 适合录屏展示

---

## 17. 需要 Codex 优先完成的任务

### P0：必须完成

1. 把现有推理代码封装为统一服务函数；
2. 完成 FastAPI 后端；
3. 完成图片检测接口；
4. 完成视频检测接口；
5. 完成基础前端页面；
6. 完成结果展示页；
7. 完成模型启动加载与错误处理；
8. 完成部署脚本；
9. 完成 README 与运行说明。

### P1：建议完成

1. 历史记录模块；
2. 示例数据演示模块；
3. 关键帧可视化；
4. 热力图保存；
5. 日志系统；
6. SQLite 存储。

### P2：可选优化

1. 多模型切换；
2. 批量检测；
3. 用户登录；
4. 结果导出 PDF；
5. 更精美的前端页面。

---

## 18. 对 Codex 的明确开发要求

请严格按以下要求继续完善系统：

1. **保持后端为 FastAPI**；
2. **不要随意更换我的模型推理逻辑，只做工程化封装**；
3. **优先保证图片/视频检测接口能跑通**；
4. **前端以“够用、清晰、适合比赛演示”为目标，不要过度设计**；
5. **目录结构必须清晰，便于我继续修改**；
6. **代码需包含必要注释、错误处理、配置文件**；
7. **必须支持本地启动和服务器启动两种模式**；
8. **部署时优先考虑单机运行，不要引入过重的微服务架构**；
9. **所有路径配置应放到配置文件或环境变量中**；
10. **输出一个可直接运行的 MVP 版本**。

---

## 19. 运行方式建议

### 19.1 本地运行

```bash
pip install -r requirements.txt
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

### 19.2 服务器运行

```bash
pip install -r requirements.txt
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

若使用 Nginx，可将公网入口代理到 8000 端口。

---

## 20. 测试要求

### 20.1 功能测试

- 图片上传是否成功
- 视频上传是否成功
- 模型推理是否正常返回
- 结果页是否正常显示
- 历史记录是否可查看

### 20.2 异常测试

- 上传空文件
- 上传错误格式
- 模型文件缺失
- GPU 不可用
- 视频解码失败

### 20.3 演示测试

- 使用 3 组固定样例做完整演示
- 每次演示总耗时可控
- 页面在录屏中显示清楚

---

## 21. 比赛展示建议

系统最终展示时，建议准备以下材料：

1. **系统首页**
2. **图片检测演示**
3. **视频检测演示**
4. **关键帧或热力图说明**
5. **模型原理简述图**
6. **系统架构图**
7. **技术亮点总结**

建议你的讲解逻辑为：

- 为什么要做深度伪造检测；
- 你的模型解决了什么问题；
- 系统如何完成从上传到输出的完整流程；
- 相比只给模型权重，你现在给出的是一个可运行、可演示、可部署的完整作品。

---

## 22. 当前最小可交付版本（MVP）定义

满足以下条件即可认为 MVP 完成：

- 能启动 Web 服务；
- 能上传图片并检测；
- 能上传视频并检测；
- 能展示结果；
- 能读取已有模型权重；
- 能在服务器上运行；
- 有 README 文档；
- 有演示样例。

---

## 23. 后续可扩展方向

- 多模态检测（图像 + 视频 + 音频）
- 伪造类型分类
- 伪造区域定位
- 批量数据集评测面板
- 模型对比实验页面
- 面向司法/媒体/教育场景的专题版本

---

## 24. 一句话交付目标

> 请基于我现有的模型权重、训练代码和推理代码，帮我做一个可运行、可部署、适合比赛演示的深度伪造检测系统；优先完成图片/视频检测闭环，技术栈以 FastAPI + 轻量前端为主，工程结构清晰，便于我后续继续修改和参赛提交。

